<!DOCTYPE html>
<html lang="en">
  <head>
    <title>House Of Ham</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%# <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%# <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100 text-gray-900">
    <%= render 'shared/header' %>
    <main class="container mx-auto mt-10 px-5">
      <% flash.each do |key, value| %>
      <div class="alert alert-<%= key %> bg-<%= flash_class(key) %>-100 border border-<%= flash_class(key) %>-400 text-<%= flash_class(key) %>-700 px-4 py-3 rounded relative mb-4" role="alert">
        <strong class="font-bold"><%= key.capitalize %>!</strong>
        <span class="block sm:inline"><%= value %></span>
      </div>
    <% end %>
      <%= yield %>
    </main>
    <%= render 'shared/footer' %>
  </body>
</html>
